# The path for the browser that the frontend uses. I personally use Ungoogled Chromium.
# ! Only Chrome-based browsers allowed.
browser = "browser/chrome.exe"

# The path of your tor installation 
tor = "tor"

# Tor SOCKS port. Change it when you need to spawn multiple NoSnoop instances.
torPort = 32022

# Receive server port. Change it when you need to spawn multiple NoSnoop instances.
receivePort = 32023